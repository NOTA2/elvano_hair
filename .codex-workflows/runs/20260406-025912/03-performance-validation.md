# 성능 검증

- 실행 명령: `npm run validate:performance`
- 결과: 통과

# 관찰 사항

- `/admin` 총합은 360 KB, 전용 자산은 16.8 KB로 보고됐다.
- `/admin/documents` 는 373 KB로 가장 무거운 관리자 라우트 후보로 유지됐다.
- 이번 변경은 모바일 GNB와 드로어 구조 변경이라 번들 급증이나 신규 대형 자산 추가는 확인되지 않았다.

# 판단

- 서버/클라이언트 경계 변경 없음
- 추가 fetch 없음
- 드로어 상태는 클라이언트 로컬 상태만 사용
- 성능 회귀 징후 없음
