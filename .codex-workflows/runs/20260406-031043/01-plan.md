# 실행 계획

1. Playwright로 관리자 화면을 열고 데스크톱/모바일에서 현재 증상을 재현한다.
2. 데스크톱에 남는 요소가 어떤 DOM인지 확인하고 숨김 규칙 우선순위를 수정한다.
3. 모바일에서는 네비게이션을 별도 고정 레이어로 올려 드로어와 백드롭이 본문 위에 오도록 정리한다.
4. Playwright와 `npm run build`로 회귀를 확인한다.
