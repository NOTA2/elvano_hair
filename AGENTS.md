# 개발용 멀티 에이전트 지침

이 저장소에서 큰 작업을 진행할 때는 아래 순서의 멀티 에이전트 워크플로를 기본으로 사용한다.

1. 기획 에이전트
2. 개발 에이전트
3. 코드 성능 검증 에이전트
4. 디자인 검증 에이전트

## 가장 중요한 규칙

- 코드 변경은 오직 개발 에이전트만 할 수 있다.
- 기획 에이전트는 코드를 읽고 계획만 작성한다.
- 코드 성능 검증 에이전트는 코드를 읽고 검증만 한다.
- 디자인 검증 에이전트는 화면과 스타일을 검토하고 검증만 한다.
- 문서와 아티팩트는 모두 한국어로 작성한다.

## 저장소 공통 맥락

- 프레임워크는 Next.js App Router와 React 19다.
- 관리자 화면은 `app/admin` 아래에 있고, 고객 공개 서명 화면은 `app/s/[token]`에 있다.
- 핵심 서버 로직은 `lib/`에 있다.
- DB 접근은 서버에서만 수행해야 하며, 클라이언트에서 Supabase를 직접 호출하지 않는다.
- 현재 기본 검증 게이트는 `npm run build`다.
- `npm run lint`는 ESLint 미설정 상태면 인터랙티브 초기 설정으로 진입할 수 있으므로 기본 게이트로 강제하지 않는다.

## 역할별 책임

### 1. 기획 에이전트

- 사용자 요청을 구현 가능한 작업 단위로 분해한다.
- 영향 범위, 가정, 리스크, 검증 계획을 먼저 정리한다.
- 코드 수정은 절대 하지 않는다.

### 2. 개발 에이전트

- 기획 결과를 바탕으로 실제 구현을 수행한다.
- 가장 작은 diff로 요구사항을 만족시키는 방향을 우선한다.
- 필요하면 더 나은 구조, 더 좋은 디자인, 더 나은 성능 개선을 함께 반영한다.

### 3. 코드 성능 검증 에이전트

- `npm run validate:performance`와 코드 리뷰를 통해 성능 회귀를 점검한다.
- 서버/클라이언트 경계, 번들 크기, 중복 fetch, 불필요한 연산, 과한 클라이언트 번들을 중점적으로 본다.
- 코드 수정은 절대 하지 않는다.

### 4. 디자인 검증 에이전트

- 화면이 깨지지 않는지뿐 아니라, 예쁘고 완성도 있게 보이는지도 평가한다.
- 색감, 타이포그래피, 여백 리듬, 시각 계층, 모바일 대응, 상태 UI를 함께 본다.
- 브라우저 도구를 사용할 수 있으면 실제 화면을 우선 확인한다.
- 코드 수정은 절대 하지 않는다.

## 실행 규칙

- 순서는 반드시 `기획 -> 개발 -> 코드 성능 검증 -> 디자인 검증`을 따른다.
- 코드 성능 검증과 디자인 검증은 개발 에이전트가 끝난 뒤에만 실행한다.
- 검증 단계에서 심각한 문제가 발견되면 그대로 승인하지 않는다.

## 저장소 가드레일

- `/admin/login`과 `/admin/(protected)`의 보호 구조를 깨지 않는다.
- 고객 공개 서명 플로우(`/s/[token]`, `app/api/documents/*`)와 관리자 기능을 혼동하지 않는다.
- Bizgo 발송, 권한 변경, 문서 생성 로직은 회귀 위험이 큰 영역으로 취급한다.
- 기존 사용자 변경사항을 임의로 되돌리지 않는다.
- 검증 도구가 막히거나 테스트가 없으면, 통과처럼 말하지 않고 막힌 이유를 명시한다.

## 기본 산출물

각 실행은 아래 아티팩트를 남긴다.

- `00-task.md`
- `01-plan.md`
- `02-build.md`
- `03-performance-validation.md`
- `04-design-validation.md`

기본 저장 위치는 `.codex-workflows/runs/<timestamp>/` 이다.

## 성능 검증 도구

- 명령: `npm run validate:performance`
- 리포트 스크립트: [`/Users/user/projects/workspace/elvano_hair/scripts/route-performance-report.mjs`](/Users/user/projects/workspace/elvano_hair/scripts/route-performance-report.mjs)
